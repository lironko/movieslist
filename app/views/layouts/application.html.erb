<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AGNI KAI | <%= content_for?(:title) ? yield(:title) : "" %></title>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <%= render 'layouts/header' %>
  </head>
  <body>
   <div class="container">
    <%= bootstrap_flash %>
    <div class="hero-unit">
     <%= yield %>
    </div>
    <%= render 'layouts/footer' %>     
  </div> <!-- /container -->
  </body>
</html>
